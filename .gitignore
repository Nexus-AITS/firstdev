node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*
yarn-error.log*

# environment — real keys never belong in the repo. The VITE_ anon key is
# public by design (it is only as powerful as the RLS policies behind it),
# but a .env can also hold values that are not, so the whole family is ignored.
.env
.env.*
!.env.example

# supabase credentials — never commit
.env
.env.*
!.env.example

# supabase CLI local state (schema SQL files ARE tracked)
supabase/.temp/
supabase/.branches/

