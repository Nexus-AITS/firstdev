# Supabase project used for Google sign-in. Vite loads this file
# automatically in dev/preview; the CSP origin in vite.config.js is derived
# from VITE_SUPABASE_URL too — change the project here, not in code.
#
# ⚠ VITE_ vars are inlined into the PUBLIC browser bundle. Only the
#   anon / publishable key may ever go here (it grants nothing beyond what
#   Row Level Security allows). NEVER put a service-role key or the
#   database connection string (it carries the DB password) below.
#
# Get the key: Supabase dashboard → Project Settings → API Keys →
# "anon" "public" (starts with eyJ… or sb_publishable_…).

VITE_SUPABASE_URL=https://xvteqcvvjlxhwijwxbbq.supabase.co
VITE_SUPABASE_ANON_KEY=

# ─────────────────────────────────────────────────────────────────────────────
# Kept for reference only. This is the DATABASE connection string — it is NOT
# the anon key and must never get a VITE_ prefix (Vite ignores non-VITE vars,
# so it stays out of the bundle). It contains the DB password: rotate it if it
# has ever been shared or committed.
# SUPABASE_DB_CONNECTION_STRING=
